# token headless: kunci mentah, jangan pernah ikut di-commit
.agent-room-token

# riwayat token: data pemakaian pribadi, bukan kode
token-riwayat.jsonl
token-riwayat.arsip.jsonl

# buku agenda: log event harian (metadata), data lokal, bukan kode
agenda/
